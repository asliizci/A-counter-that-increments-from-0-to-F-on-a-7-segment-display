# A-counter-that-increments-from-0-to-F-on-a-7-segment-display
This application creates a counter that increments from 0 to F when one button is pressed and decrements from F to 0 when another button is pressed. The current value is displayed in hexadecimal format on a 7-segment display.
